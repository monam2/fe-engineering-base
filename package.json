{
  "name": "fe-engineering-base",
  "packageManager": "yarn@4.10.3",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/@fe-base/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.0",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  }
}
